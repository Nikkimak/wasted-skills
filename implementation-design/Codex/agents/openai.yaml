interface:
  display_name: "Implementation Design"
  short_description: "Design only needed technical contracts"
  default_prompt: "Use $implementation-design to decide whether this approved PRD needs a technical design, draft it if justified, and ask me before any cross-model review."
