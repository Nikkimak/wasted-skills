interface:
  display_name: "Implementation Design"
  short_description: "Design only changed shared technical contracts"
  default_prompt: "Use $implementation-design to identify the material delta and create or amend only the shared technical and security contract that actually changes."
