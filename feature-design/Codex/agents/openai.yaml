interface:
  display_name: "Feature Design"
  short_description: "Triage fixes or create and revise PRDs"
  default_prompt: "Use $feature-design to determine whether this change needs PRD work, keeping fixes lightweight."
